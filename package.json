{
    "name": "plugin-metric-prometheus",
    "version": "0.1.0",
    "license": "Apache-2.0",
    "description": "Ale Plugin Metric Prometheus",
    "main": "lib/index.js",
    "types": "lib/index.d.ts",
    "private": true,
    "scripts": {
        "dev": "npm run build && LOG_LEVEL=info aled dev",
        "build": "rimraf lib && tsc",
        "watch": "npm run build && tsc -w --preserveWatchOutput",
        "lint": "prettier --write ."
    },
    "devDependencies": {
        "@ale-run/runtime": "latest",
        "@types/node": "^22.5.1",
        "cross-env": "^7.0.2",
        "prettier": "^2.0.4",
        "ts-node-dev": "^2.0.0",
        "tslint": "^6.1.3",
        "typescript": "^5.5.4"
    },
    "dependencies": {
        "axios": "^1.8.1",
        "prometheus-query": "^3.4.1",
        "ansi-colors": "^4.1.3"
    }
}
